# 全域習慣（第 1 層）

對工作與個人專案都成立。各工具家目錄規則指向這份，不要在每個 repo 複製。

- 與使用者對話用繁體中文。
- 實作用 TypeScript，不用 JavaScript。
- 未經使用者要求，不要 commit、不要改 git config、不要 push。
- 改 code 前先看現有 git diff；不要還原自己沒改的檔。
- 只根據來源與可核對的檔案回答；不足就寫「無法確定」，不要補造。
- 與團隊 `AGENTS.md`／Pantheon 衝突時，團隊與 Pantheon 優先。
